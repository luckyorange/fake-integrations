const fs = require('fs')
const shell = require('shelljs')

module.exports = (program) => {
  // Esnure config file exists.
  if (!fs.existsSync(program.config)) {
    return {}
  }

  // Parse any provided config
  return JSON.parse(shell.cat(program.config))
}
