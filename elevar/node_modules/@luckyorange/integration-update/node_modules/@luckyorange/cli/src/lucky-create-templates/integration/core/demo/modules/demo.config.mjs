export const controls = [
  /**
   * TODO:
   * Add controls to demo site here.
   * See demo control below:
   */
  {
    id: 'demo-button',
    name: 'Fire demo event',
    handler () {
      window.__demo('Hello, world!')
    }
  }
]

export const libraries = [
  /**
   * TODO:
   * Add demo integration library mocks here.
   * See example below:
   */
  {
    name: 'demoLibrary',
    mock: {
      property: '__demo',
      handler (...parameters) {
        return parameters
      }
    }
  }
]

export const mocks = [
  /**
   * TODO:
   * Add function mocks here.
   * These will always be loaded when the demo site loads.
   * Syntax is the name as the libraries syntax
   */
]
