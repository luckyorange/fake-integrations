import { Client } from '@elastic/elasticsearch';
export { Client, Client as EsClient } from '@elastic/elasticsearch';
import SecretManager from '@luckyorange/secret-manager';

declare const endpointKeyMap: {
    forever: "LO_EL_ENDPOINT_FOREVER";
    '30': "LO_EL_ENDPOINT_30";
};
type EndpointKeys = keyof typeof endpointKeyMap;
declare class ElasticSearch {
    private static instance;
    private secretManager;
    private clients;
    private constructor();
    static createInstance(secretManager: SecretManager): ElasticSearch;
    static getInstance(): ElasticSearch;
    initializeClients(): Promise<Partial<Record<"forever" | "30", Client>>>;
    initializeClient(endpoint: EndpointKeys): Promise<Client>;
    getClient(endpointKey: EndpointKeys): Promise<Client>;
}

export { ElasticSearch, ElasticSearch as default };
