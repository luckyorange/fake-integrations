"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const fs_1 = __importDefault(require("fs"));
const updatePackageJson = (path, fields) => {
    const packageJson = JSON.parse(fs_1.default.readFileSync(path, 'utf8'));
    Object.entries(fields).forEach(([key, value]) => {
        let currentField = packageJson[key] || {};
        switch (typeof value) {
            case 'object':
                currentField = Object.assign(Object.assign({}, currentField), value);
                break;
            case 'string':
                currentField = value;
                break;
            default:
                break;
        }
        packageJson[key] = currentField;
    });
    try {
        fs_1.default.writeFileSync(path, JSON.stringify(packageJson, null, 2), 'utf8');
    }
    catch (error) {
        console.log('An error has occurred ', error);
    }
};
exports.default = updatePackageJson;
