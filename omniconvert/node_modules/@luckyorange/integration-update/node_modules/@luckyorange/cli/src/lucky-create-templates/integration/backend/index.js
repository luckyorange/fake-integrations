require('./debug')

const IntegrationBackend = require('@luckyorange/utilities-integration-backend')
const config = require('./src/{{name}}.js')

try {
  const backend = new IntegrationBackend(config)
} catch (ex) {
  console.error(ex)
}
