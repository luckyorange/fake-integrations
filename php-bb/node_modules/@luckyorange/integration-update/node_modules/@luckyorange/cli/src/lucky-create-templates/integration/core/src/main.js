import { IntegrationCore } from '@luckyorange/integration-core'

/**
 * Welcome! Create your new {{ name }} integration here.
 */

const instance = new IntegrationCore({
  libraries: [
    /**
     * TODO:
     * Add integration libraries here.
     * See the Lucky Orange developer docs to get started.
     */
  ]
})

instance.init('{{ name }}')
