if (!process.env.LUCKY_DEV) {
  const pkg = require('./package.json')
  require('@google-cloud/debug-agent').start({
    projectId: process.env.LO_GCP_PROJECT_ID,
    serviceContext: {
      service: pkg.name,
      version: pkg.version
    }
  })
}

const { proxy } = require('@luckyorange/utilities-microservice')
proxy()
