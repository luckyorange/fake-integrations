module.exports = {
  id: '{{ name }}',  
  events: [
    {      
      names: ['*'],
      async handler ({ event, payload }, { siteId, credentials }) {         
        // TODO: Do something with the event payload
          
        // Remove event from Pub/Sub
        event.ack()        
        return true
      }
    }
  ]
}