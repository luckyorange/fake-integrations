const nock = require('nock')

nock(process.env.LO_ELASTICSEARCH_ENDPOINT)
  .persist()
  .post(() => true)
  .reply(200, (uri, requestBody) => requestBody)

nock(process.env.LO_ELASTICSEARCH_ENDPOINT)
  .persist()
  .get(() => true)
  .reply(200)
