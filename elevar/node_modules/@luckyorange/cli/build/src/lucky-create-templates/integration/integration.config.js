module.exports = {
  id: '{{name}}',
  labels: {
    name: '{{name}}',
    description: 'Connect Lucky Orange to {{name}}'
  },
  images: {
    icon: ''
  },
  authentication: {
    type: 'none'
  },
  settings (s) {
    return s.object()
      .keys({
        v1: s.object()
          .keys({
          })
      })
  }
}
