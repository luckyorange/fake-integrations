const fs = require('fs')
const shell = require('shelljs')

module.exports = (program) => {
  const options = program.opts()

  // Ensure config file exists.
  if (!fs.existsSync(options.config)) {
    return {}
  }

  // Parse any provided config
  return JSON.parse(shell.cat(options.config))
}
